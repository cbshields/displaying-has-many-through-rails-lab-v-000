<h1>Patients</h1>
<ul>

<% @patients.each do |patient| %>
<li>
  <%= link_to patient.name, patient_path(patient) %>  <%= pluralize(patient.appointments.count, 'Appointments') %><br>
</li>
<% end %>
</ul>
